language: go

go:
  - 1.4
  - 1.5

before_install:
    - go get github.com/mattn/gom

matrix:
  allow_failures:
    - go: 1.5
  fast_finish: true

script:
  - make

notifications:
  email: false

branches:
  except:
    - /^release_.*$/
